const checks = state => state.checks
const check = state => state.check
const vehicles = state => state.vehicles
const firefighters = state => state.firefighters
const itemsEquips = state => state.itemsEquips

export {
  checks,
  check,
  vehicles,
  firefighters,
  itemsEquips
}
