const setChecks = (state, checks) => {
  state.checks = checks
}

const setCheck = (state, check) => {
  state.check = check
}

const setVehicles = (state, vehicles) => {
  state.vehicles = vehicles
}

const setFirefighters = (state, firefighters) => {
  state.firefighters = firefighters
}

const setItemsEquips = (state, itemsEquips) => {
  state.itemsEquips = itemsEquips
}

export {
  setChecks,
  setCheck,
  setVehicles,
  setFirefighters,
  setItemsEquips
}
